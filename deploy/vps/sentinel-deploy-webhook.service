[Unit]
Description=Sentinel API deploy webhook
After=network.target docker.service
Requires=docker.service

[Service]
Type=simple
EnvironmentFile=/opt/sentinel/webhook.env
ExecStart=/usr/bin/python3 /opt/sentinel/webhook-server.py
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
